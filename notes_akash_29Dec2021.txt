UPDATE share SET value=value+5;


REST API
	@PutMapping("/updateShare")
	updateShare()
	{
		float rnd=Math.random()*5;
		
	}




show shares
	update button


1) 


jpa repository
	@Query("UPDATE share SET value=value+:value")
	public void updateShares(@Param("value") float value);

2) service
	public void updateShares()
	{
		float rnd=Math.random()*5;	//casting if required
		sr.updateShares(rnd);
	}
		here, generate random number and pass

3) controller
	create a url mapping for this method and call the service
	here, no need for any parameter. Because random number is generated by the service.
	@PutMapping("/updateShare")
	public void updateShares()
	{
		ss.updateShares();
	}




backup21Dec2021Jagv1		folder
